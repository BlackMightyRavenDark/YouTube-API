<UserControl x:Class="YouTubeApiLib.GuiTestWPF.NumericUpDown"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:YouTubeApiLib.GuiTestWPF"
			 x:Name="root"
			 mc:Ignorable="d" 
			 d:DesignWidth="80" d:DesignHeight="20">
	<UserControl.Resources>
		<local:IntStringConverter x:Key="intStringConverter" />
	</UserControl.Resources>

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition Width="16" />
		</Grid.ColumnDefinitions>

		<TextBox Name="textBlockNumber"
				 Text="{Binding Value, ElementName=root, Converter={StaticResource intStringConverter}}"
				 VerticalContentAlignment="Center"
				 Grid.Column="0"
				 LostFocus="textBlockNumber_LostFocus"
				 PreviewKeyDown="textBoxNumber_PreviewKeyDown" />
		<Grid Grid.Column="1">
			<Grid.RowDefinitions>
				<RowDefinition />
				<RowDefinition />
			</Grid.RowDefinitions>

			<Button Name="btnIncrease" Content="+" FontSize="8" Padding="0" Grid.Row="0"
					Click="btnIncrease_Click" ToolTip="Увеличить" />
			<Button Name="btnDecrease" Content="-" FontSize="8" Padding="0" Grid.Row="1"
					Click="btnDecrease_Click" ToolTip="Уменьшить" />
		</Grid>
	</Grid>
</UserControl>
